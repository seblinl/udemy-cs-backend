const { PI, square, add } = require("./math"); //Script
const { subtract, divide } = require("./maths"); //Directory

console.log("Math script:", PI, square(4), add(4, 3));
console.log("Maths directory:", subtract(5, 2), divide(6, 3));
